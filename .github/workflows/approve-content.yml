name: Manually publish a merged draft

on:
  workflow_dispatch:
    inputs:
      run_date:
        description: "Draft date in YYYY-MM-DD"
        required: true
        type: string
      publish_knowledge:
        description: "Approve the knowledge article"
        required: true
        default: true
        type: boolean
      publish_news:
        description: "Approve the Web news article"
        required: true
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: publish-content
  cancel-in-progress: false

jobs:
  approve:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm

      - name: Convert approved drafts to site content
        run: node scripts/approve-draft.mjs
        env:
          RUN_DATE: ${{ inputs.run_date }}
          APPROVE_KNOWLEDGE: ${{ inputs.publish_knowledge }}
          APPROVE_NEWS: ${{ inputs.publish_news }}
          APPROVED_BY: ${{ github.actor }}

      - name: Validate approved content and site build
        run: |
          npm ci
          npm run content:validate
          npm run build

      - name: Save approved content
        run: |
          git config user.name "web-direction-lab-bot"
          git config user.email "actions@users.noreply.github.com"
          git add automation/approved automation/published content
          if git diff --cached --quiet; then
            echo "No content changes to commit."
          else
            git commit -m "Manually publish approved content for ${{ inputs.run_date }}"
            git pull --rebase origin main
            git push origin main
          fi
